## Run mongodb server (port 27107)

### in case db quieries fails

```
use test
db.createCollection("users")
db.createCollection("comments")
db.createCollection("places")
db.createCollection("media")
```
